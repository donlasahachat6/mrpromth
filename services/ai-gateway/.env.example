# FastAPI AI Gateway environment variables

# Supabase service role key with write access for backend tasks.
SUPABASE_SERVICE_ROLE_KEY="supabase-service-role-key"

# Supabase project URL for database interactions.
SUPABASE_URL="https://your-supabase-project.supabase.co"

# Shared secret that Next.js API uses to authenticate with the gateway.
GATEWAY_API_KEY="replace-with-strong-shared-secret"

# Encryption secret for API key storage (32-byte base64 or hex string recommended).
ENCRYPTION_KEY="replace-with-32-byte-secret"

# Default model provider configuration.
OPENAI_API_BASE="https://api.openai.com/v1"
ANTHROPIC_API_BASE="https://api.anthropic.com"

# Local development defaults
PORT=8000

# Allowed frontend origins (comma separated when expanded).
CORS_ORIGINS="http://localhost:3000"