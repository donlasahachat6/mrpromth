# คู่มือการตั้งค่าสิทธิ์ Admin

เอกสารนี้จะแนะนำวิธีการตั้งค่าบัญชีผู้ใช้ของคุณให้มีสิทธิ์เป็น **Admin** เพื่อให้สามารถเข้าถึงหน้า Admin Dashboard ได้

## ปัญหา: 403 - ไม่มีสิทธิ์เข้าถึง

หลังจากที่คุณ login เข้าสู่ระบบแล้ว แต่ไม่สามารถเข้าหน้า Admin ได้ และเจอข้อความ "403 - ไม่มีสิทธิ์เข้าถึง" นั่นเป็นเพราะบัญชีของคุณยังไม่มี `role` เป็น `admin` ในฐานข้อมูล

## วิธีการตั้งค่าสิทธิ์ Admin

ทำตามขั้นตอนต่อไปนี้เพื่ออัปเดตสิทธิ์ของบัญชีคุณใน Supabase:

### ขั้นตอนที่ 1: ไปที่ Supabase Dashboard

- ล็อกอินเข้าสู่ [Supabase Dashboard](https://supabase.com/dashboard)
- เลือกโปรเจกต์ของคุณ

### ขั้นตอนที่ 2: ไปที่ตาราง `profiles`

1.  ในเมนูด้านซ้าย คลิกที่ไอคอน **Table Editor** (รูปตาราง)
2.  เลือกตาราง **`profiles`** จากรายการตารางทั้งหมด

    ![Supabase Table Editor](https://i.imgur.com/YOUR_TABLE_EDITOR_SCREENSHOT.png) <!-- Placeholder -->

### ขั้นตอนที่ 3: ค้นหาและแก้ไขข้อมูลบัญชีของคุณ

1.  ในตาราง `profiles` คุณจะเห็นรายชื่อผู้ใช้ทั้งหมด
2.  **ค้นหาแถวที่เป็นบัญชีของคุณ** (คุณสามารถดูจากคอลัมน์ `email` หรือ `full_name`)
3.  เมื่อเจอแถวของคุณแล้ว ให้ **ดับเบิลคลิกที่คอลัมน์ `role`** ของแถวนั้น

    ![Edit Role Column](https://i.imgur.com/YOUR_EDIT_ROLE_SCREENSHOT.png) <!-- Placeholder -->

### ขั้นตอนที่ 4: เปลี่ยนค่า `role` เป็น `admin`

1.  หลังจากดับเบิลคลิก จะมีช่องให้แก้ไขข้อความ
2.  **เปลี่ยนค่าจาก `user` เป็น `admin`**
3.  กด **Save** ที่มุมขวาล่างของหน้าจอเพื่อบันทึกการเปลี่ยนแปลง

    ![Save Role Change](https://i.imgur.com/YOUR_SAVE_ROLE_SCREENSHOT.png) <!-- Placeholder -->

### ขั้นตอนที่ 5: ทดสอบการเข้าถึงหน้า Admin

1.  กลับไปที่แอปพลิเคชัน Mr. Prompt ของคุณ
2.  **Logout** แล้ว **Login** ใหม่อีกครั้ง
3.  ตอนนี้คุณควรจะสามารถเข้าถึงหน้า Admin Dashboard ได้แล้ว! 🎉

## สรุป

การตั้งค่าสิทธิ์ Admin เป็นขั้นตอนสำคัญเพื่อให้คุณสามารถจัดการระบบทั้งหมดได้ หากคุณทำตามคู่มือนี้แล้วยังพบปัญหาอยู่ กรุณาตรวจสอบให้แน่ใจว่า:

- คุณได้แก้ไข `role` ของบัญชีที่ถูกต้อง
- คุณได้กด **Save** เพื่อบันทึกการเปลี่ยนแปลงแล้ว
- คุณได้ **Logout** และ **Login** ใหม่แล้ว

เพียงเท่านี้คุณก็จะสามารถใช้งานฟังก์ชัน Admin ได้อย่างเต็มรูปแบบครับ!
